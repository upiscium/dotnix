---
description: Systematically diagnose and fix a reported symptom
agent: build
---

Load the `task-run` skill to run `debug $ARGUMENTS`. If this is not a dedicated task worktree, request `/task-start debug $ARGUMENTS` first.
