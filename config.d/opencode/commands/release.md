---
description: Prepare or publish a repository release safely
agent: build
---

Load the `release` skill and process $ARGUMENTS. Require explicit confirmation before publishing, pushing a tag, or uploading assets.
