---
description: Diagnose and minimally fix CI failures
agent: build
---

Load the `task-run` skill to run `fix-ci $ARGUMENTS`. If this is not a dedicated task worktree, request `/task-start fix-ci $ARGUMENTS` first.
