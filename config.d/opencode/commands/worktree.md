---
description: Prepare and hand off a task to an isolated Git worktree
agent: build
---

Load the `worktree-task` skill and prepare or describe a safe isolated worktree for $ARGUMENTS, then report the required workspace handoff. Do not bypass external-directory restrictions or remove any worktree or branch without explicit confirmation.
