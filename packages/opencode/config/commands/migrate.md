---
description: Migrate a system or interface to a specified target
agent: build
---

Load the `migrate` skill and migrate $ARGUMENTS. Stop when the target, compatibility window, or rollback plan is unclear.
