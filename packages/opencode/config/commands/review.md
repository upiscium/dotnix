---
description: Review the current change or specified PR
agent: plan
---

Load the `review-change` skill for `$ARGUMENTS`. If no arguments are supplied, default to the current branch against its detected base branch. Keep the workflow read-only.
